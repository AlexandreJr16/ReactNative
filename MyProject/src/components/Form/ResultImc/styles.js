import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignSelf: "center",
    margin: 10,
  },
  txt: {
    fontSize: 16,
    fontWeight: "600",
  },
});
export default styles;
